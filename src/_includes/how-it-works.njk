<section id="how-it-works" aria-labelledby="section--how-it-works" class="l-primary">
  <h2 id="section--how-it-works">How it works</h2>
  <p class="t-headline">{{ how.title }}</p>
  
  <ol class="grid--list">
    {% for item in how.list %}
    <li>
      <h3>{{ item.title }}</h3>
      <p>{{ item.description }}</p>
    </li>
    {% endfor %}
  </ol>
</section>
